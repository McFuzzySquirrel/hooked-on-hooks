{
  "name": "hooked-on-hooks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "shared/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run --workspaces build",
    "typecheck": "npm run --workspaces typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke:e2e": "tsx scripts/smoke-e2e.ts",
    "serve:ingest": "tsx -e \"import { createIngestServer } from './packages/ingest-service/src/index.ts'; (async () => { const s = await createIngestServer(); await s.listen({ host: '127.0.0.1', port: 7070 }); console.log('INGEST_READY http://127.0.0.1:7070'); })();\"",
    "emit:event": "tsx scripts/emit-event-cli.ts",
    "session:list": "tsx scripts/export-session-store.ts",
    "session:export": "tsx scripts/export-session-store.ts",
    "bootstrap:repo": "tsx scripts/bootstrap-existing-repo.ts",
    "unbootstrap:repo": "tsx scripts/unbootstrap-existing-repo.ts",
    "replay:jsonl": "tsx scripts/replay-jsonl.ts",
    "import:vscode-chat": "tsx scripts/import-vscode-chat.ts",
    "refresh:vscode-chat-dashboard": "tsx scripts/import-export-vscode-chat.ts",
    "refresh:vscode-chat-dashboard:local": "tsx scripts/import-export-vscode-chat.ts --workspaceStorageRoot /home/mcfuzzysquirrel/.config/Code/User/workspaceStorage/01ef85096ab81119bd4d9556f2375eb5 --mode auto",
    "refresh:copilot-cli-dashboard": "tsx scripts/refresh-copilot-cli-dashboard.ts",
    "export:jsonl-dashboard": "tsx scripts/export-jsonl-dashboard.ts",
    "check:screenshot-safety": "tsx scripts/check-screenshot-safety.ts",
    "lint": "echo 'Lint config baseline in place'",
    "clean": "rm -rf coverage dist .tmp"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "playwright": "^1.59.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^4.0.2"
  }
}
